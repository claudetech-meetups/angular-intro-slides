##  Leaves

### Static website build tool

Depends only on NodeJS.

Uses:

* Yeoman
* Grunt
* Bower
* Jade (or EJS)
* Stylus (or less or plain CSS)
* Coffee (or plain JS)


```sh
$ npm install -g leaves
$ leaves setup
```
